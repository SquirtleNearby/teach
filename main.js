import 'reset-css'
import './style.css'


// Задача с превращением первых символов строки в заглавные буквы
// let text = "How can mrrrors be real if our eyes aren't real"
// function Uppercase(row) {
//   return row.split(" ").map((word) => {
//     return word.charAt(0).toUpperCase() + word.slice(1)
//   }).join(' ')
// }
// console.log(Uppercase(text));
